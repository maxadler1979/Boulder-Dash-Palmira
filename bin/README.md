# Готовые образы для Emu80

| Файл | Платформа в Emu80 |
|------|-------------------|
| `blddash.rkl` | Радио-86РК **Пальмира** |
| `blddash.rkr` | штатный **Радио-86РК** 32 КБ |
| `blddash.rka` | **Апогей БК-01** |

Сборка обновляет эти файлы из корня / `RK86/` / `Apogee/` (`compile.bat`).
